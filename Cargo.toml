[package]
name = "voyager-disco"
version = "0.5.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
clap_complete = "4"
hidapi = "2"
anyhow = "1"
toml = "1.0"

[target.'cfg(target_os = "linux")'.dependencies]
udev = "0.9"
